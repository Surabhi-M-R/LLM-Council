[project]
name = "llm-council"
version = "2.0.0"
description = "LLM Council — AWS-Integrated Multi-LLM Deliberation Platform"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # Core web framework
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.9.0",
    # AWS SDK
    "boto3>=1.35.0",
    "botocore>=1.35.0",
]
